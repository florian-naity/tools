---
import BaseLayout from '../layouts/BaseLayout.astro';
import DropZone from '../components/DropZone.astro';
---

<BaseLayout title="Merge PDFs Online — Free, No Upload" description="Combine multiple PDF files into one in the order you choose. Free, instant, and runs entirely in your browser — your files stay on your device.">

  <!-- ─── Header ───────────────────────────────────────── -->
  <div class="mb-10 animate-fade-in-up">
    <a href="/" class="back-link">All Tools</a>
    <div class="mt-4 flex items-start gap-4">
      <div class="shrink-0 w-14 h-14 rounded-2xl flex items-center justify-center text-2xl bg-orange-500/10 border border-orange-500/25 shadow-lg shadow-orange-500/5">
        📎
      </div>
      <div>
        <p class="text-xs font-bold uppercase tracking-widest text-orange-400 mb-1">PDF Tools</p>
        <h1 class="text-2xl font-bold leading-tight">Merge PDFs</h1>
        <p class="text-sm text-base-content/55 mt-1 max-w-md">
          Upload multiple PDFs, drag them into the order you want, and download them as a single combined file. Your files never leave your device.
        </p>
      </div>
    </div>
  </div>

  <div class="flex flex-col gap-6">

    <div class="animate-fade-in-up" style="animation-delay: 80ms;">
      <div class="relative rounded-2xl border border-orange-500/20 bg-base-200 overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-br from-orange-500/5 via-transparent to-transparent pointer-events-none"></div>
        <div class="relative p-5">
          <DropZone id="pdf-input" accept="application/pdf" multiple={true} label="Click to upload PDFs or drag & drop" />
        </div>
      </div>
    </div>

    <div id="editor" class="hidden flex flex-col gap-4 animate-fade-in-up" style="animation-delay: 120ms;">
      <p class="text-xs font-bold uppercase tracking-widest text-base-content/30">Files</p>
      <div class="flex items-center justify-between flex-wrap gap-2 px-1">
        <p id="file-count" class="text-sm text-base-content/50"></p>
        <div class="flex gap-2">
          <button id="add-more" class="btn btn-ghost btn-xs text-base-content/40">+ Add more</button>
          <input id="add-more-input" type="file" accept="application/pdf" multiple class="hidden" />
        </div>
      </div>

      <!-- File list with drag-to-reorder -->
      <div class="rounded-xl border border-orange-500/15 bg-base-200/50 p-3 flex flex-col gap-2">
        <div id="file-list" class="flex flex-col gap-2"></div>
      </div>

      <button id="merge-btn" class="btn btn-primary w-full">Merge & Download</button>
      <p id="merge-status" class="text-sm text-base-content/50 text-center hidden"></p>
    </div>

  </div>

  <div class="mt-10 pt-6 border-t border-base-300 flex flex-col sm:flex-row items-center justify-between gap-2">
    <p class="text-xs text-base-content/30">Your files never leave your device.</p>
    <p class="text-xs text-base-content/25">Powered by <a href="https://github.com/Hopding/pdf-lib" class="underline hover:text-primary transition-colors" target="_blank" rel="noopener">pdf-lib</a> (MIT).</p>
  </div>
  <!-- ─── How it works + FAQ ──────────────────────────────── -->
  <div class="mt-16 flex flex-col gap-10 border-t border-base-300 pt-10">

    <section>
      <p class="text-xs font-bold uppercase tracking-widest text-orange-400 mb-5">How it works</p>
      <ol class="flex flex-col gap-4">
        <li class="flex gap-3 items-start">
          <span class="shrink-0 w-6 h-6 rounded-full bg-orange-500/15 border border-orange-500/25 text-xs font-bold text-orange-400 flex items-center justify-center mt-0.5">1</span>
          <p class="text-sm text-base-content/70 leading-relaxed">Upload two or more PDF files using the drop zone or drag & drop.</p>
        </li>
        <li class="flex gap-3 items-start">
          <span class="shrink-0 w-6 h-6 rounded-full bg-orange-500/15 border border-orange-500/25 text-xs font-bold text-orange-400 flex items-center justify-center mt-0.5">2</span>
          <p class="text-sm text-base-content/70 leading-relaxed">Drag the file rows to set the order they should appear in the final document.</p>
        </li>
        <li class="flex gap-3 items-start">
          <span class="shrink-0 w-6 h-6 rounded-full bg-orange-500/15 border border-orange-500/25 text-xs font-bold text-orange-400 flex items-center justify-center mt-0.5">3</span>
          <p class="text-sm text-base-content/70 leading-relaxed">Click "Merge & Download" — the combined PDF is saved instantly to your device.</p>
        </li>
      </ol>
    </section>

    <section>
      <p class="text-xs font-bold uppercase tracking-widest text-orange-400 mb-4">Frequently asked questions</p>
      <div class="flex flex-col divide-y divide-base-300">
        <details class="py-4 group">
          <summary class="cursor-pointer list-none text-sm font-semibold text-base-content/80 flex justify-between items-center gap-4">
            Is this tool free to use?
            <span class="group-open:rotate-180 transition-transform shrink-0 text-base-content/30 text-xs">▾</span>
          </summary>
          <p class="mt-3 text-sm text-base-content/55 leading-relaxed">Yes, completely free with no limits and no sign-up required.</p>
        </details>
        <details class="py-4 group">
          <summary class="cursor-pointer list-none text-sm font-semibold text-base-content/80 flex justify-between items-center gap-4">
            Do my files get sent to a server?
            <span class="group-open:rotate-180 transition-transform shrink-0 text-base-content/30 text-xs">▾</span>
          </summary>
          <p class="mt-3 text-sm text-base-content/55 leading-relaxed">No. All merging happens in your browser using pdf-lib. Your files never leave your device.</p>
        </details>
        <details class="py-4 group">
          <summary class="cursor-pointer list-none text-sm font-semibold text-base-content/80 flex justify-between items-center gap-4">
            Is there a file size or page limit?
            <span class="group-open:rotate-180 transition-transform shrink-0 text-base-content/30 text-xs">▾</span>
          </summary>
          <p class="mt-3 text-sm text-base-content/55 leading-relaxed">There is no hard limit. Very large files may take a moment to process depending on your device.</p>
        </details>
        <details class="py-4 group">
          <summary class="cursor-pointer list-none text-sm font-semibold text-base-content/80 flex justify-between items-center gap-4">
            Can I merge more than two PDFs?
            <span class="group-open:rotate-180 transition-transform shrink-0 text-base-content/30 text-xs">▾</span>
          </summary>
          <p class="mt-3 text-sm text-base-content/55 leading-relaxed">Yes — add as many files as you like using the "+ Add more" button after the initial upload.</p>
        </details>
        <details class="py-4 group">
          <summary class="cursor-pointer list-none text-sm font-semibold text-base-content/80 flex justify-between items-center gap-4">
            Do password-protected PDFs work?
            <span class="group-open:rotate-180 transition-transform shrink-0 text-base-content/30 text-xs">▾</span>
          </summary>
          <p class="mt-3 text-sm text-base-content/55 leading-relaxed">No, password-protected files are not supported. Remove the password first using a local PDF application, then merge here.</p>
        </details>
      </div>
    </section>

  </div>

  <script is:inline type="application/ld+json">{
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      { "@type": "Question", "name": "Is this tool free to use?", "acceptedAnswer": { "@type": "Answer", "text": "Yes, completely free with no limits and no sign-up required." } },
      { "@type": "Question", "name": "Do my files get sent to a server?", "acceptedAnswer": { "@type": "Answer", "text": "No. All merging happens in your browser using pdf-lib. Your files never leave your device." } },
      { "@type": "Question", "name": "Is there a file size or page limit?", "acceptedAnswer": { "@type": "Answer", "text": "There is no hard limit. Very large files may take a moment to process depending on your device." } },
      { "@type": "Question", "name": "Can I merge more than two PDFs?", "acceptedAnswer": { "@type": "Answer", "text": "Yes — add as many files as you like using the '+ Add more' button after the initial upload." } },
      { "@type": "Question", "name": "Do password-protected PDFs work?", "acceptedAnswer": { "@type": "Answer", "text": "No, password-protected files are not supported. Remove the password first using a local PDF application." } }
    ]
  }</script>

</BaseLayout>

<script>
  import { PDFDocument } from 'pdf-lib';

  const editor = document.getElementById('editor') as HTMLDivElement;
  const fileList = document.getElementById('file-list') as HTMLDivElement;
  const fileCount = document.getElementById('file-count') as HTMLParagraphElement;
  const mergeBtn = document.getElementById('merge-btn') as HTMLButtonElement;
  const mergeStatus = document.getElementById('merge-status') as HTMLParagraphElement;
  const addMore = document.getElementById('add-more') as HTMLButtonElement;
  const addMoreInput = document.getElementById('add-more-input') as HTMLInputElement;

  interface FileEntry { id: string; file: File; }
  let entries: FileEntry[] = [];
  let counter = 0;
  let dragSrcId: string | null = null;

  function updateCount() {
    const n = getOrderedEntries().length;
    fileCount.textContent = `${n} file${n !== 1 ? 's' : ''}`;
    if (n === 0) editor.classList.add('hidden');
  }

  function getOrderedEntries(): FileEntry[] {
    return Array.from(fileList.querySelectorAll<HTMLDivElement>('.file-row'))
      .map(row => entries.find(e => e.id === row.dataset.entryId)!)
      .filter(Boolean);
  }

  function renderRow(entry: FileEntry) {
    const row = document.createElement('div');
    row.className = 'file-row flex items-center gap-3 bg-base-200 border border-base-300 hover:border-primary/30 rounded-lg px-4 py-3 cursor-grab transition-colors';
    row.draggable = true;
    row.dataset.entryId = entry.id;

    const handle = document.createElement('span');
    handle.className = 'text-base-content/30 select-none text-lg';
    handle.textContent = '⠿';

    const name = document.createElement('span');
    name.className = 'flex-1 text-sm truncate text-base-content/80';
    name.textContent = entry.file.name;

    const size = document.createElement('span');
    size.className = 'text-xs text-base-content/30 shrink-0';
    size.textContent = `${(entry.file.size / 1024).toFixed(0)} KB`;

    const removeBtn = document.createElement('button');
    removeBtn.className = 'btn btn-ghost btn-xs text-base-content/30 hover:text-error shrink-0';
    removeBtn.textContent = '✕';
    removeBtn.setAttribute('aria-label', `Remove ${entry.file.name}`);
    removeBtn.addEventListener('click', () => {
      entries = entries.filter(e => e.id !== entry.id);
      row.remove();
      updateCount();
    });

    // Drag events
    row.addEventListener('dragstart', () => {
      dragSrcId = entry.id;
      row.classList.add('opacity-40');
    });
    row.addEventListener('dragend', () => {
      row.classList.remove('opacity-40');
      fileList.querySelectorAll('.file-row').forEach(r => r.classList.remove('border-primary'));
    });
    row.addEventListener('dragover', (e) => {
      e.preventDefault();
      if (dragSrcId !== entry.id) row.classList.add('border-primary');
    });
    row.addEventListener('dragleave', () => row.classList.remove('border-primary'));
    row.addEventListener('drop', (e) => {
      e.preventDefault();
      row.classList.remove('border-primary');
      if (dragSrcId && dragSrcId !== entry.id) {
        const srcRow = fileList.querySelector(`[data-entry-id="${dragSrcId}"]`);
        if (srcRow) {
          const rows = Array.from(fileList.children);
          const si = rows.indexOf(srcRow as Element);
          const di = rows.indexOf(row);
          fileList.insertBefore(srcRow, si < di ? row.nextSibling : row);
        }
      }
    });

    row.append(handle, name, size, removeBtn);
    fileList.appendChild(row);
  }

  function addFiles(files: FileList) {
    Array.from(files).forEach(file => {
      if (file.type !== 'application/pdf') return;
      const entry = { id: `f-${++counter}`, file };
      entries.push(entry);
      renderRow(entry);
    });
    editor.classList.remove('hidden');
    updateCount();
  }

  document.addEventListener('dropzone:files', (e: Event) => {
    addFiles((e as CustomEvent<FileList>).detail);
  });

  addMore.addEventListener('click', () => addMoreInput.click());
  addMoreInput.addEventListener('change', () => {
    if (addMoreInput.files?.length) addFiles(addMoreInput.files);
  });

  mergeBtn.addEventListener('click', async () => {
    const ordered = getOrderedEntries();
    if (ordered.length < 2) {
      mergeStatus.textContent = 'Add at least 2 PDF files to merge.';
      mergeStatus.classList.remove('hidden');
      return;
    }

    mergeBtn.disabled = true;
    mergeStatus.textContent = 'Merging…';
    mergeStatus.classList.remove('hidden');

    try {
      const merged = await PDFDocument.create();
      for (const entry of ordered) {
        const bytes = await entry.file.arrayBuffer();
        const doc = await PDFDocument.load(bytes);
        const pages = await merged.copyPages(doc, doc.getPageIndices());
        pages.forEach(p => merged.addPage(p));
      }

      const pdfBytes = await merged.save();
      const blob = new Blob([pdfBytes.buffer as ArrayBuffer], { type: 'application/pdf' });
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a');
      a.href = url; a.download = 'merged.pdf'; a.click();
      URL.revokeObjectURL(url);

      const totalPages = (await PDFDocument.load(pdfBytes)).getPageCount();
      mergeStatus.textContent = `Done — ${ordered.length} files merged into ${totalPages} pages.`;
    } catch (err) {
      mergeStatus.textContent = 'Error: ' + (err as Error).message;
    } finally {
      mergeBtn.disabled = false;
    }
  });
</script>
